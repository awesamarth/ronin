---
title: Known Gaps
description: Current boundaries and production gaps.
---

# Known Gaps

Ronin's current build is real, but it is still a local-first prototype.

## Deployment

The dashboard, database, GitHub webhook URL, Slack connector, Telegram connector, and Centaur execution backend still need a production deployment plan.

The local demo runs with:

- Dashboard on `localhost:3004`
- Docs on `localhost:3005/docs`
- Slack Socket Mode connector
- Telegram long-polling connector
- Centaur execution backend

## Tenant model

The operator console uses allowlisted GitHub OAuth, but it still mostly uses the first configured org. Customer-facing operation needs org switching, install ownership, and membership roles beyond the deployment-wide allowlist.

## Background work

GitHub runs are atomically claimed from PostgreSQL by a worker and stale runs can safely replay the same Centaur idempotency keys. Support messages still run inline in their connector process; high-volume deployments may move those turns to the same worker pattern.

## Centaur service identity

Centaur currently requires an admin- or Console-capable credential for Ronin's `ronin:*` session keys. A dedicated prefix-scoped Ronin ingress identity should replace that broad credential upstream.

## Execution policy profiles

The current path supports JS/Next repos via the execution agent.

Production Ronin should detect repo stacks and apply policy profiles for ecosystems such as Python, Rust, Go, and Java.

## Support routing UI

Slack and Telegram mappings are DB-backed and functional, but the UI is still operator-oriented.

A customer-ready flow should guide users through selecting a workspace, channel, repo, and allowed actions without exposing too much internal vocabulary.
